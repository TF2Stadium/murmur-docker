machine:
  services:
    - docker

deployment:
  master:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
      - docker build -t vibhavp/murmur .
      - docker push vibhavp/murmur
